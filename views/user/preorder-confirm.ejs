<!DOCTYPE html>
<html lang="en">
<head>
	<%- include("../components/baselinks") -%>
</head>
<body>
	<%- include("../components/header") -%>
	<div class="container">
    <% if(response.success && response.itemName && response.quantity) { %>
      <div class="alert alert-success" role="alert">
        <p>Success! We've added <b><%= response.quantity %></b> count(s) of <b><%= response.itemName %></b> to your cart.</p>
      </div>
      <a href="/items/" type="button" class="btn btn-primary">Back to Catalog</a>
    <% } else {%>
      <div class="alert alert-danger" role="alert">
        <p>Sorry, there was an error with your request. Please try again later or contact us.</p>
      </div>
      <a href="/items/" type="button" class="btn btn-primary">Back to Catalog</a>
    <% } %>
  </div>
  <%- include("../components/scripts") -%>
</body>

</html>